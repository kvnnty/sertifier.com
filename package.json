{
  "name": "my-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api start:dev",
    "build": "pnpm -r build",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "pnpm": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5"
  }
}